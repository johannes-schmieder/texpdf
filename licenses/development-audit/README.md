# Development license evidence

This directory records the source-bound license audit for the bundle currently
embedded by `main`. It is intentionally separate from `licenses/generated`,
which remains the frozen audit evidence for the qualified release candidate.

The development audit may establish that the newer selection is license-clean,
but it does not qualify that selection on every supported runtime and does not
advance the active release-candidate source.
